"use client";
import Link from "next/link";import {useState} from "react";import {API_URL,getErrorMessage,saveAuth} from "@/lib/api";
export default function LoginPage(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[message,setMessage]=useState("");async function handleLogin(e:React.FormEvent){e.preventDefault();setMessage("");if(!email||!password){setMessage("Email and password are required.");return}try{const formData=new URLSearchParams();formData.append("username",email);formData.append("password",password);const res=await fetch(`${API_URL}/auth/token`,{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded"},body:formData});const data=await res.json();if(!res.ok){setMessage(getErrorMessage(data,"Login failed."));return}saveAuth(data.access_token,data.user_name,data.email);window.location.href="/dashboard"}catch{setMessage("Backend is not reachable. Start FastAPI backend first.")}}return <main className="auth-wrap"><div className="card auth-card"><span className="badge">JWT Login</span><h2>Welcome back</h2><p>Login to access your AI workspace and analysis history.</p><form onSubmit={handleLogin}><label>Email</label><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><label>Password</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="btn btn-primary" style={{width:"100%"}}>Login</button></form>{message&&<p className="error">{message}</p>}<p>New user? <Link href="/register">Create account</Link></p></div></main>}
